import React from "react";

const LoginControls = () => {
  return (
    <nav>
      <a href="#Sign-in">Sign in</a>
      <a href="#Sign-up">Sign up</a>
    </nav>
  );
};

export default LoginControls;
